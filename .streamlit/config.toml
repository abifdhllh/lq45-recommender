# Digunakan lokal & Streamlit Community Cloud (beberapa opsi di Cloud bisa di-override lewat UI).

[browser]
gatherUsageStats = false

[theme]
base = "dark"
primaryColor = "#3fb984"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#161b22"
textColor = "#fafafa"
font = "sans serif"
